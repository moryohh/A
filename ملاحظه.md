# ملاحظه — تسليم ومتابعة مشروع A

## 1. تعريف المشروع

هذا هو مشروع **A**، منصة تعليمية مبنية باستخدام React وTypeScript وVite، مع Supabase، ويتم نشرها من مستودع GitHub التالي:

- المستودع: https://github.com/moryohh/A
- المسار المحلي في بيئة العمل: `/home/ubuntu/project-a-audit`
- الفرع المستخدم: `main`
- آخر commit مرفوع إلى GitHub: `0e9bc59 feat: apply revised gamification scoring rules`
- رابط GitHub لآخر commit: https://github.com/moryohh/A/commit/0e9bc59

## 2. ما تم إنجازه في هذه الجولة

تم تطبيق نظام النقاط الجديد وربطه بتدفق التطبيق والأنشطة التعليمية. أصبحت قاعدة المستوى تبدأ من **20 نقطة في المستوى الأول**، مع زيادة قدرها **9%** في متطلبات كل مستوى تالٍ. توجد دوال لحساب النقاط التراكمية المطلوبة والوضع الحالي للاعب ونسبة التقدم داخل المستوى.

تم تطبيق قواعد الجوائز التالية في `src/services/pointsService.ts`:

| النشاط | القاعدة الحالية |
|---|---|
| الامتحان الوزاري/السنوي | الدرجة من 100 إذا كانت 50% أو أكثر، والنقاط تقسم على رقم المحاولة مع سقف القسمة عند 4؛ المحاولة الأولى 1:1، والثالثة 1:3، والرابعة وما بعدها 1:4 |
| الامتحان الشهري | 90% فأكثر = 60 نقطة، و80–89% = 50، و70–79% = 40، و50–69% = 30، وأقل من 50 = صفر؛ الإعادة تقسم النقاط على 4 |
| الامتحان اليومي | النقاط مأخوذة من الدرجة من 10؛ المحاولة الأولى 1:1 والإعادة تقسم على 4 |
| الألعاب والأنشطة | إتمام 100% = 5 نقاط، وإتمام 50–99% = 3 نقاط، وأقل من 50% = صفر؛ الإعادة تقسم على 4 |

تمت إضافة دعم صريح للمصدر `ministry_exam` والدالة `getMinistryExamReward` داخل خدمة النقاط، مع إبقاء واجهات الدوال القديمة المتوافقة مع المكونات الحالية.

تم ربط تحديث النقاط في المكونات التالية حسب آخر commit:

- `src/App.tsx`
- `src/components/AdventureWorldMap.tsx`
- `src/components/ChapterExamIcons.tsx`
- `src/components/LessonGamesModal.tsx`
- `src/components/SubjectLearningPathView.tsx`
- `src/services/pointsService.ts`

## 3. ما تم التحقق منه

تم تشغيل اختبار القواعد في الملف المحلي:

- `/home/ubuntu/test_points_rules.mjs`

وتم التحقق من أمثلة المستوى والامتحان الشهري والوزاري واليومي والإعادات. كما تم تشغيل:

```bash
cd /home/ubuntu/project-a-audit
./node_modules/.bin/tsc --noEmit
npm run build
```

والنتيجة كانت ناجحة. تم إنشاء ملفات البناء، ومنها:

- `/home/ubuntu/project-a-audit/dist/index.html`
- `/home/ubuntu/project-a-audit/dist/sw.js`

ظهر تحذير من Vite بأن بعض ملفات JavaScript أكبر من 500KB بعد التصغير، لكنه **تحذير غير مانع للبناء**، وانتهى البناء بنجاح.

تم أيضًا تشغيل `git diff --check` ولم تظهر أخطاء مسافات أو تعارضات تنسيق، ثم تم دمج تحديثات GitHub الجديدة بواسطة rebase وحل التعارضات، وبعد ذلك نجح الدفع إلى GitHub. حالة المستودع بعد الدفع كانت نظيفة، والفرع المحلي والبعيد يشيران إلى commit `0e9bc59`.

## 4. ما لم يكتمل بعد

لم يتم تأكيد النشر على Cloudflare Pages في هذه الجولة. محاولة استخدام Wrangler توقفت لأن Wrangler طلب تسجيل دخول OAuth تفاعليًا، ثم انتهت المهلة قبل إكمال التفويض:

```text
Attempting to login via OAuth...
Timed out waiting for authorization code
```

لذلك لا يجوز اعتبار رابط Cloudflare محدثًا أو اعتبار الاختبار الحي ناجحًا حتى يتم إكمال تسجيل Cloudflare أو استخدام إعداد نشر مصادق عليه مسبقًا.

لم يتم إجراء اختبار حي كامل من متصفح المستخدم على رابط Cloudflare بعد آخر commit. يجب لاحقًا التحقق من:

1. ظهور النقاط الجديدة في الملف الشخصي.
2. انعكاس المستوى والنبات والدروع وفق النقاط الحالية.
3. حفظ رقم المحاولة وتمريره للأنشطة والامتحانات.
4. عمل أيقونة Messenger وفتح قائمة المحادثات.
5. إرسال واستقبال الرسائل عبر Supabase في الوقت الحقيقي.
6. الامتحان اليومي بمدة 10 دقائق وتصميم الصفحتين.
7. الامتحان الشهري والوزاري، وخصوصًا رفع عدة صور، ظهور النتيجة، وعدم بقاء شاشة الانتظار معلقة.
8. عمل fallback إلى موقع C عند فشل مسارات التصحيح الأساسية.

## 5. ملفات مهمة للمتابعة

| الملف | وظيفته |
|---|---|
| `src/services/pointsService.ts` | القواعد المركزية للمستويات والجوائز والإعادات |
| `src/App.tsx` | الحالة العامة للمستخدم والنقاط والواجهات الرئيسية |
| `src/components/ProfileView.tsx` | عرض الملف الشخصي والمستوى والنقاط والنبات والدروع؛ يجب فحصه بصريًا بعد النشر |
| `src/components/ChapterExamIcons.tsx` | الامتحانات الشهرية/الوزارية، إرسال الإجابات، الصور، التصحيح والنتيجة |
| `src/components/LessonGamesModal.tsx` | الألعاب التعليمية وتطبيق نقاط الإعادة |
| `src/components/AdventureWorldMap.tsx` | خريطة التقدم وعرض التقدم والمكافآت |
| `src/components/SubjectLearningPathView.tsx` | مسار المادة وربط عناصر الامتحانات والتقدم |
| `src/components/MessengerModal.tsx` | واجهة المحادثات الخاصة |
| `src/components/Header.tsx` | الشعار، أيقونة Messenger، وعناصر الرأس |
| `src/services/messengerService.ts` | عمليات الرسائل الخاصة مع Supabase، إذا كان الملف موجودًا في النسخة الحالية |
| `src/services/lessonsService.ts` | جلب الفصول والدروس ومحتوى الأحياء |
| `src/services/curriculumService.ts` | جلب ملفات المنهج الخاصة بالفصل والدرس |
| `src/services/gamesService.ts` | جلب الألعاب والأسئلة الخاصة بالدرس |
| `src/services/dailyExamService.ts` | إنشاء الامتحان اليومي وأسئلته |
| `vite.config.ts` | إعداد Vite وPWA |
| `public/` | الأصول العامة وملفات الأيقونات والـ manifest إن وجدت |
| `dist/` | ناتج البناء الأخير، وليس مصدر التطبيق |

## 6. أوامر المتابعة

لفحص الحالة:

```bash
cd /home/ubuntu/project-a-audit
git status
git log -5 --oneline
```

لفحص TypeScript والبناء:

```bash
cd /home/ubuntu/project-a-audit
./node_modules/.bin/tsc --noEmit
npm run build
```

لتشغيل نسخة محلية للمعاينة:

```bash
cd /home/ubuntu/project-a-audit
npm run dev
```

لتشغيل اختبار النقاط:

```bash
node /home/ubuntu/test_points_rules.mjs
```

للنشر على Cloudflare Pages، يجب أولًا إكمال مصادقة Wrangler أو ضبط حساب Cloudflare الصحيح، ثم استخدام أمر Pages المناسب، مثل:

```bash
cd /home/ubuntu/project-a-audit
npx wrangler pages project list
npx wrangler pages deploy dist --project-name <اسم-مشروع-pages>
```

لا تستخدم اسم مشروع غير مؤكد. يجب الحصول على اسم مشروع Pages الحقيقي من حساب Cloudflare أو من إعدادات GitHub Actions/Cloudflare السابقة.

## 7. ملاحظات مهمة للذكاء الاصطناعي التالي

يجب عدم استبدال `src/services/pointsService.ts` بخدمة جديدة قبل قراءة الاستدعاءات الحالية في كل ملفات `src`. هذه الخدمة لها واجهات توافق قديمة مستخدمة في الألعاب والامتحانات، وأي حذف لها قد يسبب فشل TypeScript أو تغيرًا غير مقصود في النقاط.

يجب الحفاظ على واجهة المستخدم الحالية وعدم تغيير التصميم العام إلا إذا طلب المستخدم ذلك صراحة. قبل أي تعديل جديد، شغّل `git status` واقرأ آخر commit، ثم أنشئ commit منفصلًا واضح الاسم. لا تستخدم `git push --force`؛ فقد كانت هناك تحديثات بعيدة على GitHub وتم دمجها بواسطة rebase آمن.

يجب التمييز بين نجاح البناء المحلي ونجاح النشر الحي. البناء المحلي ناجح، أما Cloudflare Pages فلم يتم تأكيده بسبب انتهاء مهلة OAuth أثناء محاولة Wrangler.

## 8. الحالة النهائية المختصرة

> **نظام النقاط عُدّل، واختبارات TypeScript والبناء المحلي نجحت، والتعديلات دُفعت إلى GitHub في commit `0e9bc59`. النشر الحي على Cloudflare لم يُتحقق منه بعد بسبب عدم اكتمال OAuth، ويجب أن تكون الخطوة التالية إكمال المصادقة ثم نشر `dist` واختبار الرابط الحي بصريًا ووظيفيًا.**

تم إعداد هذه الملاحظة بتاريخ 13 سبتمبر 2026 وفق حالة المشروع الموجودة في بيئة العمل الحالية.

## المراجع

[1]: https://github.com/moryohh/A "مستودع مشروع A على GitHub"

[2]: https://github.com/moryohh/A/commit/0e9bc59 "آخر commit لنظام النقاط"
